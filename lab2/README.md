# Лабораторная 2

## Описание

1) Написать “плохой” Dockerfile, в котором есть не менее трех “bad practices” по написанию докерфайлов
2) Написать “хороший” Dockerfile, в котором эти плохие практики исправлены
3) В Readme описать каждую из плохих практик в плохом докерфайле, почему она плохая и как в хорошем она была исправлена, как исправление повлияло на результат
4) В Readme описать 2 плохих практики по работе с контейнерами. ! Не по написанию докерфайлов, а о том, как даже используя хороший докерфайл можно накосячить именно в работе с контейнерами.

## Этапы выполнения

### 0. Анализ лабораторной 
Как всегда вначале нужно вспомнить, что такое Doker и как с ним работать. До этого мы уже несколько раз затрагивали его на других предметах, с его помощью я даже сломал себе сначала bootcamp с ubuntu и потом еще один раз ровно также виртуалку с ubuntu, но об этом расскажу в bad practises в конце.

Спросил у всезнающих: "какие практики являются плохими" и "как их можно исправить", так что теперь попробуем их протестить и исправить.

### 1. Создание структуры проекта
Сначала создадим папку для нашего проекта и все необходимые файлы. Также, чтобы было что запустить создадим веб-приложение с приветствием на Flask.

#### Создаем папку проекта
```
mkdir docker-clouds-lab2
cd docker-clouds-lab2
```
#### Создаем файлы, с которыми будем работать
```
touch Dockerfile.bad Dockerfile.good app.py requirements.txt .dockerignore
```
Теперь у нас есть 4 файла:
1. Dockerfile.bad — "плохой" (не добрый) Dockerfile
2. Dockerfile.good — "хороший" (добрый) Dockerfile
3. app.py - приложение
4. requirements.txt — его зависимости

<img width="1268" height="99" alt="image" src="https://github.com/user-attachments/assets/ae23412b-8b83-4f34-90d9-0ae2209ec50d" />

### 2. Создание приложения
#### В ```app.py``` быстренько напишем код: 
<img width="1047" height="402" alt="image" src="https://github.com/user-attachments/assets/29fd96d9-1bde-408c-9551-7dd15d9be18f" />

#### В ```requirements.txt``` добавим зависимости:
<img width="1161" height="181" alt="image" src="https://github.com/user-attachments/assets/a0d0dc1e-e1c6-44eb-8738-dbe4a51d9397" />

Отлично, приготовления завершены 🎉, теперь перейдем к Docker. 

### 3. Написание "плохого" Dockerfile

#### В Dockerfile.bad добавим код с "плохими практиками":
<img width="1119" height="443" alt="image" src="https://github.com/user-attachments/assets/607ddb2d-63fa-422d-ac94-f09a724cb42a" />

#### Проанализируем, почему же они такие "плохие":

1) *Использование тега latest (FROM ubuntu:latest).*

- Образ latest может меняться со временем. Сборка, которая работала сегодня, может сломаться завтра, потому что под этим тегом появилась новая, возможно, несовместимая версия ОС. Это нарушает принцип воспроизводимости.

2) *Установка пакетов без очистки кеша и разбивка на несколько RUN.*
```
RUN apt-get update
RUN apt-get install -y python3 python3-pip
RUN pip3 install flask
```

- Каждая инструкция RUN создает новый слой в образе. Кеш apt-get и скачанные пакеты остаются в этом слое, что увеличивает итоговый размер образа.
- Разделение apt-get update и apt-get install может привести к установке устаревших пакетов из-за кеширования слоев Docker.

3) *Копирование всего контекста сборки (COPY . /app).*

- Эта команда скопирует в образ все файлы в текущей директории, включая потенциально ненужные: Dockerfile.bad, Dockerfile.good, .git, виртуальные окружения, кеш IDE и т.д. Это снова увеличивает размер образа и может привести к утечке конфиденциальной информации.

Плохой Dockerfile готов (все как обычно), теперь сделаем хороший!

### 4. Написание "хорошего" Dockerfile
#### Исправим все найденные ошибки и добавим еще парочку "хороших" практик:
<img width="1211" height="617" alt="image" src="https://github.com/user-attachments/assets/de958d05-3dab-45be-851e-15205b1bcd34" />

1) Используем конкретный, легковесный и более безопасный образ
```
FROM python:3.11-slim-bullseye
```

2) Создаем пользователя сразу, чтобы избежать запуска от root на ранних этапах
```
RUN groupadd -r myuser && useradd -r -g myuser myuser
```

3) Устанавливаем рабочую директорию
```
WORKDIR /app
```

4) Копируем только файл с зависимостями отдельно от кода, это позволяет Docker кешировать слой с установленными зависимостями
```
COPY requirements.txt .
```

5) Устанавливаем зависимости в одном RUN, чистим кеш pip
```
RUN pip install --no-cache-dir -r requirements.txt
```

6) Меняем пользователя на непривилегированного
```
USER myuser
```

7) Копируем только нужные файлы приложения (остальное игнорируется через .dockerignore, сейчас создадим его)
```
COPY app.py .
```

8) Определяем переменные окружения для конфигурации
```
ENV NAME=Developer \
  PORT=80
```
   
9) Инструкция для "здоровья" контейнера
```
HEALTHCHECK --interval=30s --timeout=3s \
  CMD curl -f http://localhost:${PORT}/ || exit 1
```

10) Объявляем о используемом порте
```
EXPOSE ${PORT}
```

11) Запускаем приложение
```
CMD ["python", "app.py"]
```

#### Cоздадим обещанный файл .dockerignore для правильной работы COPY:
<img width="1113" height="315" alt="image" src="https://github.com/user-attachments/assets/126f9688-c029-414e-b889-d4c673776645" />

Класс 😁, хороший файл тоже готов, осталось протестировать наше творение.

### 5. Тестирование (куда без него)
#### Построим образы и посмотрим на разницу в размере:

* Для этого скачаем Docker в WSL (Ubuntu как я говорил, я сломал😢)
<img width="1020" height="121" alt="image" src="https://github.com/user-attachments/assets/4241b157-1122-4cd3-b20c-2bcdc33078ea" />

* Соберем образ плохого файла: 
```
docker build -t my-bad-app -f Dockerfile.bad .
```
* И сразу получаем подтверждение, что практика с RUNами плохая:
<img width="1823" height="848" alt="image" src="https://github.com/user-attachments/assets/b7081d30-3394-46c7-b4da-f1cc89aef574" />

* Попробуем все же сделать костыль, чтобы посмотреть на разницу в весе:
* Добавим флаг ```--break-system-packages``` в Dockerfile.bad:
  <img width="1196" height="401" alt="image" src="https://github.com/user-attachments/assets/d8df9786-6666-4983-bde5-895078af0da7" />
  
* И снова попробуем собрать его образ:
<img width="1875" height="533" alt="image" src="https://github.com/user-attachments/assets/2b35e218-20c6-4642-b457-c55a35ebb0e1" />

* Предупреждение с ENV осталось, но образ собрался:
<img width="1875" height="533" alt="image" src="https://github.com/user-attachments/assets/aa469dc5-74ea-4bbc-b6df-f2f8183f0d14" />

* Теперь соберем образ хорошего файла:
```
docker build -t my-good-app -f Dockerfile.good .
```
* Все прошло успешно с первого раза без единой ошибки:
<img width="1919" height="752" alt="image" src="https://github.com/user-attachments/assets/7028ec66-991f-42cb-a322-aea4b6055ac3" />

* Ну и проверим разницу наших образов:
```
docker images | grep my-
```
<img width="924" height="96" alt="image" src="https://github.com/user-attachments/assets/f8a0d4c0-d417-4f69-9e89-c13147583b33" />

* Как можно увидеть, разница больше чем в 400 Мб !!!

#### Теперь запустим наш контейнер (не зря же писали приложение):
* Запустим командой:
```
docker run -d -p 8080:80 --name good-container my-good-app
```
<img width="1314" height="79" alt="image" src="https://github.com/user-attachments/assets/784d0132-7e56-4263-abcc-63c0b4d83c3d" />

* Откроем в браузере по адресу http://localhost:8080:
<img width="1820" height="111" alt="image" src="https://github.com/user-attachments/assets/2ac6743d-89d9-4745-94ec-de67deb68926" />

* Замечательно, контейнер работает и здоровается с нами🤝.

### 6. Еще две плохих практики: 

1. То что сломало мне ~~жизнь~~ bootcamp и виртуалку - запуск docker в фоне без перезагрузки ```docker run -d без --restart=always```.
*Честно говоря, я точно не помню какой именно флаг я делал (потому что все сломалось) и в отчете я об этом не писал:), но по-моему этот.*
*   **Проблема:** При данном флаге контейнер завершает работу только при команде ```docker stop``` и, если во время работы у вас система начнет обновляться (как у меня), то не только контейнер сломается, но и вся ОС. (Хотя этот момент вроде как продуман разработчиками, но у меня почему-то как всегда ничего не работало. Может быть я все таки как-то не так установил сам Docker или же версия была старая.)
*   **Решение:** Запускать контейнеры точно зная, что делает каждый из флагов и анализировать точно ли этот способ больше всего вам подходит.

2. Не хранить данные внутри контейнера (то чем я скорее всего занимался бы, если щас не написал).
*   **Проблема:** Если приложение пишет данные (логи, файлы пользователей, данные БД) внутрь контейнерной файловой системы, то эти данные будут безвозвратно утеряны при остановке или удалении контейнера.
*   **Решение:** Использовать Docker Volumes или bind mounts для хранения постоянных данных. Например, `docker run -v /path/on/host:/path/in/container ...`. Это отделяет данные от жизненного цикла контейнера и позволяет обновлять приложение без потери информации.
