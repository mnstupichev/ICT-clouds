# Лабораторная 1

## Этапы выполнения

### 0. Анализ лабораторной

Спросил нашего [китайского друга](https://chat.deepseek.com)  - "Как лучше выполнять работу по шагам" и "Получиться ли выполнить работу на Windows".
Конечно же можно (я думаю так в начале отчета), так что установил WSL и настроил работу хостов.
#### Настройка hosts в Windows:
Открыл Блокнот от имени администратора

Открыл файл C:\Windows\System32\drivers\etc\hosts

Добавил строки:
```
127.0.0.1 alpha.test www.aaa.test
127.0.0.1 beta.test www.bbb.test
```
<img width="1414" height="724" alt="image" src="https://github.com/user-attachments/assets/2158f260-0522-445c-a74c-360f7112bfe5" />

Первоначальные настройки закончены, пока что все идет хорошо, настроение тоже заммечательное. :)

### 1. Подготовка структуры каталогов

Так как искать готовый пет проект было лень, я создал два небольших с парой строчек.
#### Для этого создадим папки для наших двух проектов и их логирования:
```
# Создаем корневые директории для файлов проектов
sudo mkdir -p /var/www/alpha.test/html
sudo mkdir -p /var/www/beta.test/html

# Создаем папки для логов
sudo mkdir -p /var/www/alpha.test/logs
sudo mkdir -p /var/www/beta.test/logs

# Создаем простые index.html файлы для каждого проекта
sudo nano /var/www/alpha.test/html/index.html
```
<img width="1246" height="349" alt="image" src="https://github.com/user-attachments/assets/f8b16318-496f-4a31-a55d-1c61911546d7" />

```
sudo nano /var/www/beta.test/html/index.html
```
<img width="1332" height="441" alt="image" src="https://github.com/user-attachments/assets/f6d6074a-98e4-4aac-8b77-1b28a4065571" />

```
# Устанавливаем правильные права владения
sudo chown -R www-data:www-data /var/www/alpha.test
sudo chown -R www-data:www-data /var/www/beta.test
sudo chmod -R 755 /var/www
```
<img width="972" height="276" alt="image" src="https://github.com/user-attachments/assets/3fcc557e-e1bc-4c91-ab90-cab51788e473" />

Отлично, каталоги готовы. 🎉

### 2. Генерация SSL-сертификатов (самоподписанных)

Как я понял можно взять где-то бесплатные, но сделать свои как будто бы легче, так что сгенерируем самоподписанные:

```
# Создаем директорию для сертификатов
sudo mkdir /etc/nginx/ssl
```
```
# Генерируем ключ и сертификат для alpha.test
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
    -keyout /etc/nginx/ssl/alpha.test.key \
    -out /etc/nginx/ssl/alpha.test.crt
```

<img width="1894" height="609" alt="image" src="https://github.com/user-attachments/assets/b7746013-a349-420d-9651-b9c86b097554" />
```
# Генерируем ключ и сертификат для beta.test
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
    -keyout /etc/nginx/ssl/beta.test.key \
    -out /etc/nginx/ssl/beta.test.crt
```

<img width="1904" height="717" alt="image" src="https://github.com/user-attachments/assets/cf1f4fd5-be69-4d58-83b2-1074ee447431" />

Пояснения (для себя больше):
```
-x509: Создать самоподписанный сертификат.
-nodes: Не шифровать приватный ключ (чтобы Nginx мог читать его без пароля).
-days 365: Срок действия сертификата.
```
Пока что не было ниодной ошибки, это успех. 🔥

### 3. Настройка виртуальных хостов (конфиги сайтов)


